---
name: Dropdown menu
route: /dropdown-menu
---

import {Playground} from 'docz';
import {DropdownMenu, MenuItem, Col, Row, AccountIcon} from '@medmain/react-ui-kit';

import Root from '../components/root';

# Dropdown menu

## Basic

Using the default options: `position="top"` and `alignment="left"`

<Playground>
  <Root>
    <Row>
      <Col>
        <span />
      </Col>
      <Col>
        <DropdownMenu label={'Actions'} position="top">
          <MenuItem onClick={() => console.log('Replace')}>Replace</MenuItem>
          <MenuItem onClick={() => console.log('Delete')}>Delete</MenuItem>
        </DropdownMenu>
      </Col>
      <Col>
        <DropdownMenu label={'Disabled menu'} position="top" alignment="right" disabled>
          <MenuItem onClick={() => console.log('Replace')}>Replace</MenuItem>
          <MenuItem onClick={() => console.log('Delete')}>Delete</MenuItem>
        </DropdownMenu>
      </Col>
    </Row>
  </Root>
</Playground>

Updating the options: `position="bottom"` and `alignment="right"`

<Playground>
  <Root>
    <Row>
      <Col>
        <span />
      </Col>
      <Col>
        <DropdownMenu label={'Actions'} position="bottom" alignment="right">
          <MenuItem onClick={() => console.log('Replace')}>Replace</MenuItem>
          <MenuItem onClick={() => console.log('Delete')}>Delete</MenuItem>
        </DropdownMenu>
      </Col>
      <Col>
        <span />
      </Col>
    </Row>
  </Root>
</Playground>
